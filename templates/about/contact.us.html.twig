{% extends 'main.base.html.twig' %}

{% block title %} Welcome !{% endblock %}
    {% block stylesheets %}
        {{ parent() }}
        <link href="{{ asset('css/jquery-ui.min.css') }}" rel="stylesheet">
    {% endblock %}
 {% block mainContent %}
     <div id="about-contact-us">
         <div class="alert alert-light" role="alert">
             {% if is_posted  == false %}
                 {{ include('contact_admin/_form.html.twig') }}
             {% else %}
                 <div class="text-info">Ihre Nachricht wurde gesendet</div>
             {% endif %}
         </div>
     </div>
 {% endblock %}