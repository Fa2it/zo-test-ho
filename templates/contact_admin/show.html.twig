{% extends 'base.html.twig' %}

{% block title %}ContactAdmin{% endblock %}

{% block body %}
    <h1>ContactAdmin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact_admin.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact_admin.email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact_admin.message }}</td>
            </tr>
            <tr>
                <th>Topic</th>
                <td>{{ contact_admin.topic }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contact_admin_index') }}">back to list</a>

    <a href="{{ path('contact_admin_edit', {'id': contact_admin.id}) }}">edit</a>

    {{ include('contact_admin/_delete_form.html.twig') }}
{% endblock %}
