<nav class="navbar navbar-expand navbar-light bg-white topbar mb-1 mt-2 shadow">
    {% if app.user %}
    <!-- Sidebar Toggle (Topbar) -->
    <button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
        <i class="fa fa-bars"></i>
    </button>
    {% endif %}
    <!-- Topbar Search -->
    <form class="d-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search">
        <div class="input-group">
            <div class="input-group-append">
                <a class="nav-link" href="{{ path('search_ride') }}" id="searchFormLarge">
                    <i class="fas fa-search fa-fw"></i>
                </a>
            </div>
        </div>
    </form>

    <!-- Topbar Navbar -->
    <ul class="navbar-nav ml-auto">
        <!-- Nav Item - Search Dropdown (Visible Only XS) -->
        <li class="nav-item no-arrow d-sm-none">
            <a class="nav-link" href="{{ path('search_ride') }}" id="searchForm">
                <i class="fas fa-search fa-fw"></i>
            </a>
        </li>

        {% if app.user %}
        <!-- Nav Item - User Information -->
        <li class="nav-item dropdown no-arrow">
            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                <span class="mr-1 d-none d-lg-inline text-gray-600 small">Hallo !{{ app.user.getFirstName() }}</span>
                {% if app.user.getPhoto() is not empty  %}
                <img class="img-profile rounded-circle" src="/media/thumbnail/{{  app.user.getPhoto() }} " width="30" height="30">
                    {% else %}
                        <i class="far fa-user-circle" style="font-size:1.8rem"></i>
                {% endif %}
            </a>
            <!-- Dropdown - User Information -->
            <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
                <a class="dropdown-item" href="{{ path('my_user_index') }}">
                    <i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
                    Profile
                </a>
                <a class="dropdown-item" href="{{ path('my_user_index') }}">
                    <i class="fas fa-list fa-sm fa-fw mr-2 text-gray-400"></i>
                    Dashboard
                </a>
                <a class="dropdown-item" href="#">
                    <i class="fas fa-car fa-sm fa-fw mr-2 text-gray-400"></i>
                    Auto
                </a>
                <a class="dropdown-item" href="#">
                    <i class="fas fa-bus-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                    Die Fahrt
                </a>
                <a class="dropdown-item" href="#">
                    <i class="fas fa-star-half-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                    Bewertung
                </a>
                <a class="dropdown-item" href="#">
                    <i class="fas fa-user-tie fa-sm fa-fw mr-2 text-gray-400"></i>
                    Admin
                </a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="{{ path('app_logout') }}">
                    <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                    Ausloggen
                </a>
            </div>
        </li>
            {% else %}
                <li class="nav-item no-arrow">
                    <a class="nav-link" href="{{ path('app_login') }}" >
                        <i class="fas fa-sign-in-alt"></i> Einloggen
                    </a>
                </li>
                <li class="nav-item no-arrow">
                    <a class="nav-link" href="{{ path('app_register') }}" >
                        <i class="fas fa-registered"></i> Anmeldung
                    </a>
                </li>
        {% endif %}

    </ul>

</nav>